#include"RBT.h"
#include <stdio.h>
#include <stdlib.h>

int main() {
    /* RedBlackTree tree;
    tree.root = NULL;

    insert(&tree, 10);
    insert(&tree, 20);
    insert(&tree, 30); */

    // Test the tree structure

    return 0;
}
